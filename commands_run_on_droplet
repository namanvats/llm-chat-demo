# To ensure no UI things come up during setup
sudo tee /etc/apt/apt.conf.d/99force-conf <<'EOF'
Dpkg::Options {
  "--force-confdef";
  "--force-confold";
}
EOF


No passphrase for the key
ssh-keygen -t rsa -b 4096 -f ~/.ssh/droplet_deploy_key -N ""